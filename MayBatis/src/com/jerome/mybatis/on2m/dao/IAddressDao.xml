<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jerome.mybatis.on2m.dao.IAddressDao"><!-- 命名空间  用于区分隔离 sql语句 指定的是要操作的 接口 -->
  <resultMap type="com.jerome.mybatis.on2m.entity.Address" id="addressList">
  				<id property="id" column="id"/><!-- 主键id -->
  				<result property="sid" column="sid"/>
  				<result property="provincial" column="provincial"/>
  				<result property="urban" column="urban"/>
  				<result property="area" column="area"/>
  </resultMap>
  <select id="findBysId" parameterType="Integer" resultMap="addressList">
			select * from address1 where sid=#{sid}
  </select>
  
  </mapper>