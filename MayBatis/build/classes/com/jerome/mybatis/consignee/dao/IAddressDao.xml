<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jerome.mybatis.consignee.dao.IAddressDao"><!-- 命名空间  用于区分隔离 sql语句 指定的是要操作的 接口 -->
  <resultMap type="com.jerome.mybatis.consignee.entity.Address" id="addressList">
  				<id property="aid" column="aid"/><!-- 主键id -->
  				<result property="provincial" column="provincial"/>
  				<result property="urban" column="urban"/>
  				<result property="area" column="area"/>
  </resultMap>
  <select id="findById" parameterType="Integer" resultMap="addressList">
			select * from address where aid=#{aid}
  </select>
  
  </mapper>